version: '3.7'

services:

  worker:
    build: .
    shm_size: 14gb
    command: celery -A app.scheduler.tasks:app worker -l info -c 27 -B
    depends_on:
      - redis

  # beat:
  #   build: .
  #   command: celery -A app.scheduler.tasks:app beat -l info
  #   depends_on:
  #     - redis

  redis:
    image: "redis:alpine"